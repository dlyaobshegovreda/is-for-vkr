Django>=6.0,<7.0
reportlab>=4.0       # генерация PDF (заказ-наряд, акт) — чистый Python, без системных библиотек
openpyxl>=3.1        # выгрузка отчётов в Excel

# Для продакшена на PostgreSQL раскомментируйте:
# psycopg2-binary
